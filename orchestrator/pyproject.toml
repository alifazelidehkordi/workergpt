[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "orchestrator-mvp"
version = "0.1.0"
description = "Multi-agent research orchestrator MVP"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
